node_modules
build
dist
.env
.env.*
.DS_Store
.firebase
.claude/

# web/ 랜딩을 빌드/실행 전 자동복사한 결과물 (원본은 web/)
/public/landing

# 일회성 마이그레이션 스크립트가 남기는 Firestore 백업 (이메일·전화번호 포함)
/scripts/.backup-*.json

# 데모 계정 시드 결과 (비밀번호 포함 — 절대 커밋 금지)
/scripts/.demo-*.json

# 작업 중 생성되는 임시 스크립트·스크린샷
/tmp-*.mjs
/shot__*.png
